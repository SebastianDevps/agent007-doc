---
title: "Agent007 v5.0.0"
description: "Intelligent Development Orchestration System — 8 expert agents · 25+ active skills · 12 commands · 28 hooks · autonomous pipeline · SDD-enforced"
---

<img
  className="block dark:hidden"
  src="/images/logo.png"
  alt="Agent007 Hero"
/>
<img
  className="hidden dark:block"
  src="/images/logo.png"
  alt="Agent007 Hero"
/>

## What is Agent007?

Agent007 is a Claude Code plugin that provides an **intelligent orchestration layer** on top of Claude — 8 expert agents, 25+ active skills, and 12 commands that work together to plan, implement, review, and ship code.

Every message is **automatically classified and routed** to the right expert. Every claim is **backed by evidence**. Every task follows the **SDD Iron Law**: no production code without a defined observable scenario first.

<CardGroup cols={2}>
  <Card
    title="Quickstart"
    icon="rocket"
    href="/quickstart"
  >
    Get up and running in under 5 minutes
  </Card>
  <Card
    title="Commands"
    icon="terminal"
    href="/commands/dev"
  >
    12 slash commands — from /dev to /orchestrate
  </Card>
  <Card
    title="Agents"
    icon="users"
    href="/agents/overview"
  >
    8 expert agents with tool policy enforcement
  </Card>
  <Card
    title="Changelog"
    icon="clock"
    href="/changelog"
  >
    What's new in v5.0.0
  </Card>
</CardGroup>

---

## Ecosystem at a Glance

| Category | Count | Details |
|----------|-------|---------|
| Expert agents | **8** | backend-db, code-reviewer, frontend-ux, loop-operator, platform, product, refactor-cleaner, security |
| Skills (invokable) | **19** | pipeline + domain + workflow-utils |
| Skills (auto-injected) | **6** | core + orchestration |
| Skills (total active) | **25+** | — |
| Commands | **12** | /dev, /consult, /ralph-loop, /orchestrate, /prompt-gen, /deep-research, /commit, /pull-request, /security-scan, /instinct-status, /evolve, /changelog |
| Hooks | **28** | 7 new OpenClaw primitives + 21 existing quality gates |
| Rules | **5** | typescript, security, git-workflow, patterns, coding-style |

---

## OpenClaw Primitives

7 new safety and orchestration hooks that run automatically on every session:

| Hook | Trigger | What it does |
|------|---------|--------------|
| `tool-loop-detection` | PostToolUse/all | SHA-256 fingerprint sliding window. Warning at 10 identical calls, circuit breaker at 30. |
| `context-engine` | PreToolUse/Agent + Stop | Estimates token budget before each subagent spawn. Writes `.sdlc/state/context-budget.json`. Blocks spawn if > 80%. |
| `mutation-guard` | PreToolUse/Write\|Edit\|Bash | Fingerprints writes by content hash. Silently skips exact duplicate operations. |
| `memory-decay` | SessionStart | Marks MEMORY.md entries stale at ~30 days, archives at ~60 days using exponential decay. |
| `tool-policy-guard` | PreToolUse/Write\|Edit | Enforces `tool_profile` per active agent: `minimal` / `coding` / `full`. |
| `provider-rotation` | PreToolUse/Agent | Tracks model failures with exponential cooldown. Recommends failover: opus → sonnet → haiku. |
| `transcript-policy` | SubagentStart | Injects model-tier directive: haiku gets concise mode, opus gets deep-analysis mode, sonnet is unchanged. |

---

## Routing Intelligence

Every message is classified before any action is taken:

```
🎯 [TYPE] → [ROUTE] | Risk: [low|medium|high|critical]
```

| Message type | Signal words | Route |
|---|---|---|
| feature | add, implement, create, build | `/dev` |
| bug | fix, error, broken, not working | systematic-debugging → SDD |
| consult | should I, recommend, best practice | `/consult` |
| refactor | refactor, clean up, optimize | `sop-reverse` → plan |
| review | code review, audit, check quality | `code-reviewer` |
| research | investigate, explore, compare | `/deep-research` |
| ralph | loop until, autonomous, until tests pass | `loop-operator` → `/ralph-loop` |
| cleanup | dead code, unused, depcheck | `refactor-cleaner` |

---

## Immutable Rules

These rules are always active and cannot be overridden:

- **NEVER** claim "done" without `Skill('verify')` passing with evidence
- **NEVER** write production code before defining an observable scenario (SDD Iron Law)
- **NEVER** modify scenarios to make code pass — scenarios are external holdout sets
- **NEVER** assume user approval — require explicit confirmation
- **ALWAYS** read a file before editing it
- **ALWAYS** show routing decision before any skill invocation

---

## Pipeline Map

```
simple  → generate → verify → done
          On FAIL: retry with feedback (max 3). On 3× FAIL: escalate.

medium  → plan → [generate → verify] → code-reviewer → branch-finish

complex → brainstorming → git-worktree → plan
          → [generate → verify] + ralph-loop per task
          → human gate → branch-finish

refactor → sop-reverse → plan → medium pipeline
bug      → systematic-debugging → generate → verify
```
